/**
 * Placeholder page for "Các thẻ của tôi" (My Decks).
 * Will be implemented with deck CRUD later.
 */
export function DecksPage() {
  return (
    <div className="space-y-4">
      <h2 className="text-2xl font-bold text-foreground">Các thẻ của tôi</h2>
      <p className="text-muted-foreground">
        Danh sách bộ thẻ sẽ hiển thị ở đây.
      </p>
    </div>
  );
}
